# Browser calls use the same-origin proxy, avoiding backend CORS changes.
VITE_API_BASE_URL=/api
# Development and preview proxy target. Never put credentials here.
API_PROXY_TARGET=http://127.0.0.1:8000
